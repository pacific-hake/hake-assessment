```{r load-packages-chunk, echo = FALSE}

# To install rnaturalearthhires (it was hard to find with Google):
# devtools::install_github("ropensci/rnaturalearthhires")
# devtools::install_github("pbs-assess/gfutilities")
message("Loading R packages...")
pacman::p_load(
  # Alphabetical order
  adnuts,
  cli, coda, cowplot, crayon,
  data.tree, date, dplyr,
  future, furrr,
  ggh4x, ggplot2, ggpubr, ggrepel, grDevices, grid,
  gridGraphics, gridExtra, gtools,
  here,
  # kableExtra - Using cgrandin's fork because there is a bug in the master
  # *Sending a pull request to author to fix it - Oct 2, 2023*
  kableExtra, knitr,
  lubridate,
  maps, matrixcalc,
  parallel, purrr,
  r4ss, RColorBrewer, readr, reshape2, rnaturalearth, rnaturalearthhires, rstan,
  scales, shinystan, sf, stringr,
  testthat, tictoc, tidyr, tools,
  xtable)
message("Finished loading R packages...")
```
