# Catches

## Catches
```{r main-catches-fig, fig.width = 7, out.height = "85%"}

plot_catches(ct, xlim = c(start_yr, last_data_yr),
            leg_font_size = 8,
            leg_key_size = 0.5,
            leg_ratio = c(0.5,2))
```
